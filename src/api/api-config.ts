const API_BASE_URL = 'https://dev.ntastic.site/api';

export const API_PATHS = {
  UPLOAD: `${API_BASE_URL}/upload`,
  GET_STATUS: `${API_BASE_URL}/task/`,
  DOWNLOAD: `${API_BASE_URL}/download/`,
};

export default API_BASE_URL;